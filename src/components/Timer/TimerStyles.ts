import { StyleSheet } from "react-native";

export default StyleSheet.create({
  timeView: {
    display: "flex",
    flexDirection: "row",
    justifyContent: "center",
  },
  timeText: {
    fontSize: 40,
  },
  acttionBtn: {
    backgroundColor: "#ccc",
    borderRadius: 50,
    height: 50,
    width: 100,
    alignItems: "center",
    justifyContent: "center",
    paddingBottom: 3,
    zIndex: 10,
  },
});
